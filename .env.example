# PostgreSQL
POSTGRES_USER=app
POSTGRES_PASSWORD=changeme_use_strong_password
POSTGRES_DB=training

# PostgREST JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-at-least-32-chars
